/**
 * 사람이름 + age 반환 함수
 * @todo jsdoc 익히기
 * @param {string} name 사람 이름
 * @param {number} age 사람 나이
 * @returns {string} 사람이름 +  age
 * @version 1.0.0
 * @see https://naver.com
 */
const qwe = (name, age) => {
  return name + age;
};

// JS Doc 이라고 함
// 변수나 함수 위에 작성하여 설명할 수 있음
